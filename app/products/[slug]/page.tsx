import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, CheckCircle2, FileText, MapPin, Package, ShieldCheck } from "lucide-react";
import { products } from "@/lib/site-data";
import { Badge } from "@/components/ui/badge";
import { InquiryForm } from "@/components/forms/inquiry-form";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=products.find(x=>x.slug===slug);return p?{title:p.name,description:p.description}:{title:"Product not found"}}
export default async function ProductDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();const schema={"@context":"https://schema.org","@type":"Product",name:p.name,description:p.description,countryOfOrigin:p.origin,image:`${process.env.NEXT_PUBLIC_SITE_URL||"https://tradebridge-global.example.com"}${p.image}`};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="bg-slate-50 py-5"><div className="container-site"><Link href="/products" className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={16}/>Back to products</Link></div></section><section className="container-site grid gap-10 py-12 lg:grid-cols-[1.1fr_.9fr]"><div><div className="relative aspect-[4/3] overflow-hidden rounded-2xl bg-slate-100"><Image src={p.image} alt={p.name} fill priority className="object-cover"/></div><div className="mt-4 grid grid-cols-3 gap-3">{["Main view","Inspection view","Packaging view"].map((x,i)=><div key={x} className="relative aspect-[4/3] overflow-hidden rounded-lg bg-slate-100"><Image src={p.image} alt={`${p.name} ${x}`} fill className={`object-cover ${i===1?"object-left":"object-center"}`}/><span className="absolute inset-x-0 bottom-0 bg-[#071a33]/70 px-2 py-1 text-center text-[10px] text-white">{x}</span></div>)}</div></div><div className="lg:pt-3"><div className="flex flex-wrap gap-2"><Badge>{p.category}</Badge><Badge className="bg-emerald-50 text-emerald-700">{p.status}</Badge></div><h1 className="mt-5 text-4xl font-extrabold text-[#071a33]">{p.name}</h1><p className="mt-5 text-base leading-7 text-slate-600">{p.description}</p><div className="mt-7 grid gap-3 rounded-xl border border-slate-200 p-5 sm:grid-cols-2">{[[MapPin,"Origin",p.origin],[CheckCircle2,"Condition",p.condition],[Package,"Minimum order",p.moq],[FileText,"Packaging",p.packaging]].map(([Icon,label,value])=><div key={label as string} className="flex gap-3"><Icon size={19} className="mt-0.5 text-blue-700"/><div><span className="block text-xs text-slate-500">{label as string}</span><strong className="text-sm text-slate-800">{value as string}</strong></div></div>)}</div><div className="mt-8"><h2 className="text-lg font-extrabold text-[#071a33]">Specifications</h2><dl className="mt-4 divide-y divide-slate-100 border-y border-slate-100 text-sm"><div className="grid grid-cols-3 py-3"><dt className="text-slate-500">Summary</dt><dd className="col-span-2 font-semibold">{p.spec}</dd></div><div className="grid grid-cols-3 py-3"><dt className="text-slate-500">Application</dt><dd className="col-span-2 font-semibold">{p.application}</dd></div><div className="grid grid-cols-3 py-3"><dt className="text-slate-500">Availability</dt><dd className="col-span-2 font-semibold">{p.status}</dd></div></dl></div><div className="mt-6 flex items-start gap-3 rounded-lg bg-amber-50 p-4 text-sm text-amber-900"><ShieldCheck className="shrink-0" size={19}/><span>Supplier identity and direct contact details are shared only through an approved introduction.</span></div></div></section><section id="inquiry" className="bg-slate-50 py-16"><div className="container-site max-w-4xl"><h2 className="text-3xl font-extrabold text-[#071a33]">Request a quotation</h2><p className="mt-3 text-slate-600">Tell us your destination, quantity and timing. We will confirm availability and matching commercial terms.</p><div className="mt-8 rounded-2xl border border-slate-200 bg-white p-6 md:p-8"><InquiryForm productName={p.name}/></div></div></section></>}
