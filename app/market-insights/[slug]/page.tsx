import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowRight, CalendarDays, Clock3 } from "lucide-react";
import { insightArticles,getArticleSections } from "@/lib/insights-data";
import { formatDate } from "@/lib/utils";
import { Badge } from "@/components/ui/badge";
export function generateStaticParams(){return insightArticles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=insightArticles.find(x=>x.slug===slug);return a?{title:a.title,description:a.summary}:{title:"Article not found"}}
export default async function InsightArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const article=insightArticles.find(x=>x.slug===slug);if(!article)notFound();const sections=getArticleSections(article);const related=insightArticles.filter(a=>a.slug!==article.slug).sort((a,b)=>Number(b.category===article.category)-Number(a.category===article.category)).slice(0,3);const schema={"@context":"https://schema.org","@type":"Article",headline:article.title,datePublished:article.date,description:article.summary,image:`${process.env.NEXT_PUBLIC_SITE_URL||"https://tradebridge-global-jp-in.chandey.chatgpt.site"}${article.image}`,publisher:{"@type":"Organization",name:"TradeBridge Global"}};return <article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><header className="bg-[#071a33] py-16 text-white"><div className="container-site max-w-5xl"><Link href="/market-insights" className="inline-flex items-center gap-2 text-sm font-bold text-blue-200"><ArrowLeft size={16}/>Market Insights</Link><div className="mt-8 flex flex-wrap items-center gap-4"><Badge>{article.category}</Badge><span className="flex items-center gap-1.5 text-sm text-slate-400"><CalendarDays size={15}/>{formatDate(article.date)}</span><span className="flex items-center gap-1.5 text-sm text-slate-400"><Clock3 size={15}/>{article.readingTime}</span></div><h1 className="mt-5 max-w-4xl text-4xl font-extrabold leading-tight md:text-6xl">{article.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-300">{article.summary}</p></div></header><div className="container-site grid max-w-6xl gap-10 py-12 lg:grid-cols-[240px_1fr]"><aside className="lg:sticky lg:top-28 lg:h-fit"><div className="rounded-xl border border-slate-200 bg-slate-50 p-5"><strong className="text-sm text-[#071a33]">Table of contents</strong><nav className="mt-4 grid gap-2">{sections.map((section,index)=><a key={section.id} href={`#${section.id}`} className="text-xs leading-5 text-slate-600 hover:text-blue-700"><span className="mr-2 font-mono text-slate-400">{String(index+1).padStart(2,"0")}</span>{section.title}</a>)}</nav></div></aside><div><div className="relative aspect-[16/8] overflow-hidden rounded-2xl"><Image src={article.image} alt="" fill priority className="object-cover"/></div><div className="mt-10 space-y-10">{sections.map(section=><section key={section.id} id={section.id} className="scroll-mt-28"><h2 className="text-2xl font-extrabold text-[#071a33]">{section.title}</h2><div className="mt-4 space-y-4 text-[16px] leading-8 text-slate-700">{section.paragraphs.map((paragraph,index)=><p key={index}>{paragraph}</p>)}</div></section>)}</div><blockquote className="mt-12 rounded-2xl bg-blue-700 p-7 text-white md:p-9"><p className="text-xl font-extrabold leading-8">Looking for a product, supplier, or international business opportunity? Submit your requirement and our team will review it.</p><Link href="/request" className="mt-6 inline-flex h-12 items-center gap-2 rounded-lg bg-amber-400 px-6 text-sm font-extrabold text-[#071a33]">Submit a Requirement <ArrowRight size={16}/></Link></blockquote></div></div><section className="border-t border-slate-200 bg-slate-50 py-16"><div className="container-site"><h2 className="text-3xl font-extrabold text-[#071a33]">Related articles</h2><div className="mt-8 grid gap-6 md:grid-cols-3">{related.map(a=><article key={a.slug} className="overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="relative h-44"><Image src={a.image} alt="" fill className="object-cover"/></div><div className="p-5"><Badge>{a.category}</Badge><h3 className="mt-3 text-lg font-extrabold leading-snug text-[#071a33]"><Link href={`/market-insights/${a.slug}`}>{a.title}</Link></h3><Link href={`/market-insights/${a.slug}`} className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-blue-700">Read More <ArrowRight size={14}/></Link></div></article>)}</div></div></section></article>}
