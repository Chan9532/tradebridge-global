import type { Metadata } from "next";import { requireRole } from "@/lib/auth";import { DashboardShell } from "@/components/dashboard/dashboard-shell";import { SupplierOverview } from "@/components/dashboard/supplier-overview";export const metadata:Metadata={title:"Supplier Dashboard",robots:{index:false,follow:false}};export const dynamic="force-dynamic";export default async function Page(){await requireRole(["supplier","admin"]);return <DashboardShell role="Supplier" items={["Overview","Company profile","My products","Available RFQs","Submitted offers","Messages","Documents","Profile settings"]}><SupplierOverview/></DashboardShell>}
