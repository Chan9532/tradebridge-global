import type { Metadata } from "next";import { requireRole } from "@/lib/auth";import { DashboardShell } from "@/components/dashboard/dashboard-shell";import { BuyerOverview } from "@/components/dashboard/buyer-overview";export const metadata:Metadata={title:"Buyer Dashboard",robots:{index:false,follow:false}};export const dynamic="force-dynamic";export default async function Page(){await requireRole(["buyer","admin"]);return <DashboardShell role="Buyer" items={["Overview","My RFQs","Supplier offers","Saved products","Messages","Documents","Profile settings"]}><BuyerOverview/></DashboardShell>}
