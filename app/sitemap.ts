import type{MetadataRoute}from"next";import{products,articles}from"@/lib/site-data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://tradebridge-global.example.com";const routes=["","/products","/buyer-requests","/submit-rfq","/supplier-registration","/suppliers","/sourcing-service","/market-insights","/about","/contact","/privacy","/terms","/commission-disclosure"];return[...routes.map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:url===""?"weekly" as const:"monthly" as const,priority:url===""?1:.7})),...products.map(p=>({url:`${base}/products/${p.slug}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:.8})),...articles.map(a=>({url:`${base}/market-insights/${a.slug}`,lastModified:new Date(a.date),changeFrequency:"monthly" as const,priority:.6}))]}
