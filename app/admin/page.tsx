import type { Metadata } from "next";import { requireRole } from "@/lib/auth";import { DashboardShell } from "@/components/dashboard/dashboard-shell";import { AdminConsole } from "@/components/dashboard/admin-console";import { CustomerRequestManager } from "@/components/dashboard/customer-request-manager";export const metadata:Metadata={title:"Admin Dashboard",robots:{index:false,follow:false}};export const dynamic="force-dynamic";export default async function Page(){await requireRole(["admin"]);return <DashboardShell role="Admin" items={["Overview","Customer Product Requests","Suppliers","RFQs","Products","Inquiries","Articles","Matches","Documents","Profile settings"]}><AdminConsole/><CustomerRequestManager/></DashboardShell>}
